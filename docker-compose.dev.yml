version: "3.9"

services:
  api:
    build:
      target: builder   # use builder stage with all dev deps
    volumes:
      - ./app:/app      # hot-reload on code changes
    environment:
      - ENV=development
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
